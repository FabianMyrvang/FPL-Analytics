# Internal documentation (not needed publicly)
docs/

# Python
__pycache__/
*.py[cod]
*.pyo
.ipynb_checkpoints/

# Generated output CSVs are now TRACKED and published (regenerated + pushed by the
# GitHub Action) so others can use the data. Intentionally NOT ignored.

# Local data backups
FPL_DATA_backup/

# External repo dependencies (clone separately — see README)
FPL-Core-Insights/

# vaastav archive — no longer needed locally (01_fpl_merge.py fetches directly from GitHub)
Fantasy-Premier-League/

# Generated seed data (rebuild with 01_fpl_merge.py + vaastav archive)
seed_data/

# Cache directories
pl_api_cache/
downloaded_files/

# OS
.DS_Store
Thumbs.db
